<h1>Apply for a school place</h1>

<h2>Where do you live?</h2>

<%= bootstrap_form_for(@address, method: 'get', url: schools_results_path) do |f| %>
  <%= f.search_field :postcode %>
  <%= f.search_field :name_or_number %>

  <%= f.primary 'Which schools could I apply to?', name: nil %>
<% end %>

